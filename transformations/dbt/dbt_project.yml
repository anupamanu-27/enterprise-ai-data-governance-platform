name: enterprise_ai_governance
version: "1.0.0"
config-version: 2

profile: enterprise_ai_governance

model-paths:
  - models

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  enterprise_ai_governance:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table

